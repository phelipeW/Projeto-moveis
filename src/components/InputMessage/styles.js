import { StyleSheet } from 'react-native';
import { colors } from '../../styles';

const styles = StyleSheet.create({
  text: { fontSize: 11, color: colors.tomato, marginTop: 6 },
});

export default styles;
